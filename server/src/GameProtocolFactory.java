/**
 * A factory for creating GameProtocol objects.
 */
public class GameProtocolFactory {
	
	/**
	 * Creates the game protocol.
	 *
	 * @return the game protocol
	 */
	public GameProtocol create(){
		return new GameProtocol();
	}
}
